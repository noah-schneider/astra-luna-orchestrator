interface:
  display_name: "Astra + Solx Orchestrator"
  short_description: "Thin Astra orchestration; Sol xhigh builds."
  default_prompt: "Use $astra-solx-orchestrator in its thin-root default: Astra defines the contract and accepts once; one installed Sol worker owns implementation and verification."
policy:
  allow_implicit_invocation: true
