interface:
  display_name: "Astra + Sol Orchestrator"
  short_description: "Thin Astra orchestration; Sol builds and verifies."
  default_prompt: "Use $astra-sol-orchestrator in its thin-root default: Astra defines the contract and accepts once; one installed Sol worker owns implementation and verification."
policy:
  allow_implicit_invocation: true
